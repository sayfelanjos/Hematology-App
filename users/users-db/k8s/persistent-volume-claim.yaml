apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: users-db-pvc
  labels:
    app: users-db-pvc
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
