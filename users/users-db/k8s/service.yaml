apiVersion: v1
kind: Service
metadata:
  name: users-db-service
  labels:
    app: users-db-service
spec:
  type: NodePort
  ports:
    - protocol: TCP
      nodePort: 30007
      port: 5432
      name: users-db
      targetPort: 5432
  selector:
    app: users-db-deployment
