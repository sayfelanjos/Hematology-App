apiVersion: v1
kind: Service
metadata:
  name: statistics-mf
  labels:
    app: statistics-mf
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: statistics-mf
